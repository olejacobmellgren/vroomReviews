export type CarCard = {
  car: {
    id: string;
    company: string;
    model: string;
    image: string;
  };
  rating: number;
  review: string;
  
  //vi må fikse på dette
  id: string;
  company: string;
  model: string;
  image: string;
};
